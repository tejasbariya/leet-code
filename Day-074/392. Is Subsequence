class Solution(object):
    def isSubsequence(self, s, t):
        """
        :type s: str
        :type t: str
        :rtype: bool
        """
        i = 0
        n = len(s)
        for char in t:
            if i < n and char == s[i]:
                i += 1
        return i == n 
